#pragma strict

var moveUp : KeyCode;
var moveDown : KeyCode;

var dtY : int = 20;

function Update () {
	if(Input.GetKey(moveUp)){
		// move position
	}
	else if(Input.GetKey(moveDown)){
		// move down
	}
}